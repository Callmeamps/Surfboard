# Issue Tracker

This repository uses **GitHub Issues** for tracking work.

- Create issues via `gh issue create` or the GitHub web UI
- The `bd` CLI provides a dependency-first layer on top of GitHub Issues
- All agent work uses `bd` commands; it syncs to GitHub Issues automatically