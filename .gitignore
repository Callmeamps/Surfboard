node_modules/
dist/

# Environment variables (credentials)
.env

# Mise local overrides (user-specific, not committed)
mise.local.toml
mise.lock

# Extension sources (unpacked Chrome extensions, not part of app source)
sample-extension/
storage.json
*.swp
*.bak
.DS_Store
test/profile-*.json
test/profiles.json

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
