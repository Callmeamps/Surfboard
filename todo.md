# Handoff Follow-up Tasks

## Single-session tasks

- [ ] E2E smoke test — launch Electron, verify right sidebar buttons, canvas pages, extensions load
- [ ] Verify shell shortcut `Ctrl+Shift+S` doesn't conflict with anything (was changed from `Ctrl+Shift+X`)
- [ ] Check if finding #7 (shell conflict) was properly tracked — was it captured as a fix or lost?

## Beads cross-refs

- riced-chromium-57t — Multi-profile support
- riced-chromium-omi — Canvas pages real data wiring
- riced-chromium-xow — Miniapps sandboxing
- riced-chromium-y2k — HTML-in-Canvas native rendering
