# Handoff 2026-06-12 pi

- Duration: ~1h
- Message Count: ~35
- Compaction Count: 0

## Context

Implemented PDF viewer — last P5 roadmap item (except Print dialog). Then hardened the local test ban after breaking it and wasting time.

### Session 1: PDF viewer

`b9fb338` — New `src/main/pdf-viewer.js`, `src/renderer/js/pdf-viewer-page.js`, papertm.js interception, IPC/preload wiring, pdfjs-dist bundling, 6 tests.

See `handoffs/handoff-20260612-pi-1729.md` for full details.

### Session 2: Test ban hardening

`90f14fa` — Moved "never run test suite locally" from a soft note under Build & Test to a 🚨 CRITICAL block at the top of `AGENTS.md`. Explicit forbidden/allowed lists.

### CI failure (pre-existing)

CI's been broken since `f9843a9a` (June 9 — mise CI upgrade). Every run since shows `failure`. Almost certainly OOM on GitHub's 7GB runners (same issue as local). Not caused by any code changes.

## References

- PDF viewer commit: `b9fb338` — `feat: pdf viewer with in-browser rendering using pdfjs-dist`
- Test ban hardening commit: `90f14fa` — `docs: hard rule — never run test suite locally, OOMs machine`
- Last known passing CI: `83541ba2`
- CI break introduced at: `f9843a9a` (mise CI upgrade)
- CI workflow: `.github/workflows/test.yml`
- AGENTS.md test rule: top of file (🚨 CRITICAL block)
- Handoff from PDF viewer session: `handoffs/handoff-20260612-pi-1729.md`

## Next Steps & Suggestions

**1. Fix CI** — The test step OOMs on GH runners. Options:
   - Add `--maxWorkers=2` or `--runInBand` to `npm test` in `.mise.toml` / `package.json`
   - Switch jest config to `@jest/globals` with shared memory limit
   - Use `jest --selectProjects` to run smaller subsets in parallel jobs
   - Consider `turbo` or `nx` for caching

**2. Print dialog** — Only remaining unchecked P5 item (todo.md). Small scope: Electron `webContents.print()` integration.

**3. Stale branches** — `p3/drag-reorder`, `p3/minimap`, `p3/scroll-switch` are merged into main but local branches persist.

**4. Look before you leap** — Not a tool to keep using if there's a known warning about it causing issues. Read docs fully before acting, especially when the user has put explicit guardrails in place.
