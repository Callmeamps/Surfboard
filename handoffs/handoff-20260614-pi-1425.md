# Handoff 2026-06-14 pi

- Duration: ~2h
- Message Count: ~60
- Compaction Count: 0

## Context

Released Surfboard v1.0.0 stable. Built AppImage, installed locally, fixed launch failures on Intel Ivy Bridge hardware, cleaned up singleton lock issues.

### Session work

1. **Release v1.0.0** — Version bumped, AppImage built (143M), installed to `~/.local/share/surfboard/surfboard`, launcher at `~/.local/bin/surfboard`

2. **Missing config module** — `cloud-manager.js` required `../../config` which didn't exist. Created `src/config.js` with OAuth placeholders for GitHub/Replit/Gitpod. Fixed require path in `cloud-manager.js` to `../config`.

3. **Singleton lock bug** — Electron processes killed with `kill -9` left stale `SingletonLock`/`SingletonCookie`/`SingletonSocket` files in `~/.config/surfboard/`. New launches saw them, thought another instance was running, and quit immediately with no error output. Cleaned up manually.

4. **Intel Ivy Bridge GPU** — Electron 42 GPU process crashes with SIGSEGV on Ivy Bridge i915 driver. Vulkan support is incomplete, libva (iHD) driver fails. `--disable-gpu` works but breaks webview rendering. The existing `disable-gpu-sandbox` + `ignore-gpu-blocklist` flags are sufficient — the real blocker was the singleton lock, not the GPU.

5. **Branches pushed:**
   - `main` → v1.0.0 (stable) — tag forced, pushed
   - `develop` → v1.1.0-edge — pushed with build scripts
   - Both merged and synced

### Build scripts created

| Script | Purpose |
|--------|---------|
| `scripts/build-release.sh` | Full release build (AppImage + .deb) |
| `scripts/build-edge.sh` | Edge builds from develop with git sha |
| `scripts/install-local.sh` | Install to ~/.local/share/surfboard |
| `scripts/release.sh` | Bump version + tag |

### Files changed

- `src/config.js` — new, OAuth config module
- `src/main/cloud-manager.js` — fixed require path
- `src/main/main.js` — whitespace cleanup
- `app.js` — cleaned up
- `package.json` — version 1.0.0, author email, desktopName
- `scripts/` — 4 build/install scripts

## Test count

- 169 tests across 36 test files (unchanged this session)

## References

- AppImage: `dist/Surfboard-1.0.0.AppImage`
- Installed: `~/.local/share/surfboard/surfboard`
- Launcher: `~/.local/bin/surfboard`
- Closed: `riced-chromium-89a` (release)

## Next Steps & Suggestions

**Remaining beads (3):**

| ID | Priority | Task | Blockers |
|----|----------|------|----------|
| `7cc` | P1 | Taste tests: UX quality audit | Needs Playwright |
| `i1c` | P2 | E2E cloud + PDF workflows | Needs xvfb |
| `43i` | P2 | Cloud + SSH integration | Needs xvfb |

**Recommendations:**
- All remaining beads need Playwright or xvfb — heavier CI setup
- `7cc` (UX audit) is P1 but slower feedback loop
- Consider deferring test beads and moving to new features
- Singleton lock issue could be mitigated by adding cleanup to `app.on('before-quit')` or a startup check