# Handoff 2026-06-12 pi

- Duration: ~3h
- Message Count: ~40
- Compaction Count: 0

## Context

CI hardening session. Improved test infrastructure for memory-constrained environments.

### Session 1: CI Infrastructure

`0499c89` — Composite action for shared setup, npm/mise caching, matrix strategy for light tests, path filters to skip CI on docs, status check job for branch protection.

### Session 2: Test Optimization

`5647afb` — Three improvements:
1. **Memory fix**: Removed global `setupFilesAfterEnv` that loaded all 8 feature-platform modules (4106 lines) for every test. Heavy tests now explicitly require setup. Light tests run without overhead.
2. **Diff-based testing**: `scripts/changed-tests.sh` detects affected tests from git diff. PRs run only affected tests (~2-5 min vs ~10+ min).
3. **Optimized grouping**: Moved download-manager, miniapps to light tests (no feature-platform). Reduced heavy jobs from 6 to 5.

## References

- Composite action: `.github/actions/setup/action.yml`
- CI workflow: `.github/workflows/test.yml`
- Diff detection: `scripts/changed-tests.sh`
- Feature-platform setup: `test/setup-feature-platform.js`
- Package.json jest config: `package.json` (setupFilesAfterEnv removed)

## Next Steps & Suggestions

**1. Verify CI green** — Push triggered CI. Check `gh run list` for status.

**2. Clean stale branches** — `p3/drag-reorder`, `p3/minimap`, `p3/scroll-switch` are merged but local branches persist.

**3. Consider Blacksmith** — Drop-in replacement for GitHub runners. Bare metal = better single-core for Jest. 3000 free min/mo. Just change `runs-on: blacksmith-4vcpu`.

**4. CI improvements remaining:**
- Turborepo/nx caching for test results
- Self-hosted runner if memory remains issue
- Test sharding across runners (not just grouping)

**5. Feature work** — All P5 features complete. 9 test beads open (skipped per user). Settings, canvas pages, SSH/Cloud all complete.
